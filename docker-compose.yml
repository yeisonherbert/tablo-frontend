services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api-tablo.antana.com.pe}
    image: tablo-frontend:latest
    ports:
      - "8080:80"
    restart: unless-stopped
